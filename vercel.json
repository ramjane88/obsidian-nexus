{
  "version": 2,
  "builds": [
    { "src": "api/fast_main.py", "use": "@vercel/python" },
    { "src": "frontend/package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/fast_main.py" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}
